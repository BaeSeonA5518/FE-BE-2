# 백엔드 API 베이스 URL (비우면 Vite dev 프록시 `/api` 사용)
# VITE_API_BASE_URL=http://localhost:8080

# Vite dev 서버 프록시 대상 (vite.config.js)
# VITE_API_PROXY_TARGET=http://localhost:8080

# S5 walk/stream 구동 (기본 ON). GPS만 쓰려면 false
# VITE_USE_WALK_STREAM=false
